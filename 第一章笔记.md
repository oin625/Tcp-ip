基于Linux 的文件操作

```c
int fd = open("a.txt", o_RDWR); //成功时返回文件描述符，失败时返回-1;
/*文件打开模式如下： ps.11
O_CREAT     必要时创建文件
O_TRUNC     删除全部现有数据
O_APPEND	维持现有数据，保存到其后面
O_RDONLY	只读打开
O_WRONLY	只写打开
O_RDWR		读写打开
*/
int close(int fd); //成功时返回0，失败时返回-1;


```

